<UserControl x:Class="AttendanceSystem_Windows.PageFolder.Template.CreateTemplate"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AttendanceSystem_Windows.PageFolder.Template"
             mc:Ignorable="d" Background="White"
             d:DesignHeight="480" d:DesignWidth="640">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <ScrollViewer VerticalScrollBarVisibility="Auto" 
                      Margin="45,45,45,0">
            <StackPanel Name="ContentUI">

            </StackPanel>
        </ScrollViewer>
        <Button Name="SubmitButton" Grid.Row="1" Width="60" Height="30"
                BorderThickness="0"
                Content="提交"
                Margin="50,25" 
                HorizontalAlignment="Right"
                Click="SubmitButton_Click"/>
    </Grid>
</UserControl>
